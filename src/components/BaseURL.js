// This file is used to set the base URL for the API requests
const BaseURL = `http://${window.location.hostname}/api`;
// const BaseURL = `http://rocket2/api`;
console.log(BaseURL);

export default BaseURL